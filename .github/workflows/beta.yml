on:
  push:
    branches:
      - beta

name: 🚀 Beta Deployment
jobs:
  check_comft:
    name: Check files
    outputs:
      run_job: ${{ steps.check_files.outputs.run_job }}
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v2
        with:
          fetch-depth: 2

      - name: check modified files
        id: check_files
        run: |
          echo "=============== list modified files ==============="
          git diff --name-only HEAD^ HEAD
          
          echo "========== check paths of modified files =========="
          git diff --name-only HEAD^ HEAD > files.txt
          while IFS= read -r file
          do
            echo $file
            if [[ $file != comft/* ]]; then
              echo "This modified file is not under the 'comft' folder."
              echo "::set-output name=run_job::false"
              break
            else
              echo "::set-output name=run_job::true"
            fi
          done < files.txt
  
  job_for_comft:
    name: 🎉 API Deploy
    needs: check_comft
    if: needs.check_comft.outputs.run_job == 'true'
    runs-on: ubuntu-latest
    steps:
      - name: run for 'comft' folder
        run: echo "This job runs only when all the modified files are under the 'comft' folder."
     
  check_prven:
    name: Check files
    outputs:
      run_job: ${{ steps.check_files.outputs.run_job }}
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v2
        with:
          fetch-depth: 2

      - name: check modified files
        id: check_files
        run: |
          echo "=============== list modified files ==============="
          git diff --name-only HEAD^ HEAD
          
          echo "========== check paths of modified files =========="
          git diff --name-only HEAD^ HEAD > files.txt
          while IFS= read -r file
          do
            echo $file
            if [[ $file != prven/* ]]; then
              echo "This modified file is not under the 'prven' folder."
              echo "::set-output name=run_job::false"
              break
            else
              echo "::set-output name=run_job::true"
            fi
          done < files.txt

  job_for_prven:
    name: 🎉 Web Deploy
    needs: check_prven
    if: needs.check_prven.outputs.run_job == 'true'
    runs-on: ubuntu-latest
    steps:
      - name: run for 'prven' folder
        run: echo "This job runs only when all the modified files are under the 'prven' folder."
      # - name: 🚚 Get latest code
      #   uses: actions/checkout@v2

      # - name: Use Node.js 12
      #   uses: actions/setup-node@v2-beta
      #   with:
      #     node-version: "12"

      # - name: 🔨 Build Project
      #   run: |
      #     cd prven
      #     npm install
      #     npm run build
      #   env:
      #     REACT_APP_ENV: BETA

      # - name: 📂 Sync files
      #   uses: SamKirkland/FTP-Deploy-Action@4.0.0
      #   with:
      #     server: ftp.landslide.id
      #     username: ${{ secrets.username }}
      #     password: ${{ secrets.password }}
      #     local-dir: ./prven/build/
      #     server-dir: ./beta.landslide.id/
