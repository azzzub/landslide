on:
  push:
    branches:
      - beta

name: 🚀 Beta Deployment
jobs:
  # CI optimation, run on the changed file only
  changes:
    name: 🔎 Looking for changes
    runs-on: ubuntu-latest
    outputs:
      comft: ${{ steps.filter.outputs.comft }}
      prven: ${{ steps.filter.outputs.prven }}
    steps:
      - uses: dorny/parth-filter@v2
        id: filter
        with:
          filters: |
            comft:
              - 'comft/**'
            prven:
              - 'prven/**'

  comft:
    name: 🎉 Comft Deploy
    needs: changes
    if: ${{ needs.changes.outputs.comft == 'true' }}
    runs-on: ubuntu-latest
    steps:
      - name: run for 'comft' folder
        run: echo "This job runs only when all the modified files are under the 'comft' folder."

  prven:
    name: 🎉 Prven Deploy
    needs: changes
    if: ${{ needs.changes.outputs.prven == 'true' }}
    runs-on: ubuntu-latest
    steps:
      - name: run for 'prven' folder
        run: echo "This job runs only when all the modified files are under the 'prven' folder."
      # - name: 🚚 Get latest code
      #   uses: actions/checkout@v2

      # - name: Use Node.js 12
      #   uses: actions/setup-node@v2-beta
      #   with:
      #     node-version: "12"

      # - name: 🔨 Build Project
      #   run: |
      #     cd prven
      #     npm install
      #     npm run build
      #   env:
      #     REACT_APP_ENV: BETA

      # - name: 📂 Sync files
      #   uses: SamKirkland/FTP-Deploy-Action@4.0.0
      #   with:
      #     server: ftp.landslide.id
      #     username: ${{ secrets.username }}
      #     password: ${{ secrets.password }}
      #     local-dir: ./prven/build/
      #     server-dir: ./beta.landslide.id/
